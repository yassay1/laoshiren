# 老实人｜上线最小用户与通知支持设计

> **版本**：v2.2  
> **状态**：专项技术设计基线（Minimal Production Support）  
> **适用阶段**：Backend V2 / HarmonyOS 首次正式上架  
> **更新时间**：2026-08-28  
> **上位约束**：《老实人 Backend V2 总体架构设计 v2.2》《老实人 Agent Runtime 技术设计 v2.2》  
> **目标**：在不扩张成完整账号平台/通知平台的前提下，补齐正式上架、真实用户隔离、App 关闭后的主动提醒和必要运营质量能力。

---

# 0. 文档定位

本文只解决“老实人”第一次正式上线 HarmonyOS App 所需的**最小用户与通知支持**。

本文不是企业 IAM 设计，也不是通知运营平台设计。

核心目标：

```text
华为账号登录
    ↓
稳定 internal user_id
    ↓
用户数据 owner-scoped
    ↓
最小 Device
    ↓
PushEndpoint / Token
    ↓
Automation / Reminder
    ↓
NotificationIntent / NotificationDelivery / DurableJob
    ↓
Huawei Push Kit
    ↓
锁屏 / 横幅 / 通知中心
    ↓
点击 Deep Link 回到对应事务
```

同时补齐：

```text
Logout
Account Deletion
Privacy
APMS
Cloud Testing
上架 / 激励计划人工操作清单
```

---

# 1. 设计原则

## 1.1 保证效果，但不做平台化

V2.2 只建设消费者 App 真正需要的能力。

MUST 做：

```text
Huawei Account login
internal user_id
owner isolation
minimal Device
Push Token lifecycle
Reminder Push
Deep Link
Logout
Account Deletion
Privacy inventory
release quality support
```

明确不做：

```text
自建用户名密码体系
手机号验证码体系
邮箱注册
复杂多 Provider 账号绑定
Organization / Tenant / RBAC
复杂用户资料中心
营销通知平台
A/B Push
用户分群
短信
邮件
实况窗
复杂通知中心
```

## 1.2 华为能力服务产品，不反向绑死领域

```text
Huawei Account
Huawei Push Kit
APMS
Cloud Testing
```

是正式上线适配能力。

Product Domain 仍然只认识：

```text
User
Device
Notification intent
```

不让业务领域直接依赖华为 SDK 对象。

## 1.3 外部身份不是业务主键

```text
Huawei UnionID / OpenID
≠
internal user_id
```

所有业务对象统一 owner 到老实人自己的稳定 `user_id`。

## 1.4 Push 失败不能改变业务真相

```text
Reminder due
→ Automation 已执行
→ Notification intent 已产生
```

即使 Push 暂时失败：

```text
业务事实不能 rollback
```

Push delivery 是独立外部副作用。

---

# 2. V2.2 最小技术决策

| 议题 | V2.2 决策 |
|---|---|
| 主登录方式 | Huawei Account / Account Kit |
| 登录协议 | 客户端获取 Authorization Code，服务端完成身份交换 |
| 外部身份 | 优先 UnionID；内部仍映射为 `user_id` |
| AGC Auth | **不是 V2.2 必选依赖**；不为了上线再叠一层账号系统 |
| Backend Session | Backend 颁发自己的业务登录凭证；JWT / opaque token 细节后定 |
| User Profile | 只做账号存在性和状态，不建设 Profile Domain |
| Device | 一等轻量对象，只承担 Push / timezone / last_seen |
| Reminder | Automation → Outbox → Huawei Push |
| 通知权限 | Just-in-time 请求，不在首次启动无理由索取 |
| Push Token | 独立 PushEndpoint 持有并关联 Device，可更新、失效、删除 |
| Deep Link | Reminder / Agent Result MUST 可回到对应业务上下文 |
| Delivery | MUST 记录发送结果；SHOULD 接入 Huawei delivery receipt |
| Logout | 退出当前登录，不删除用户业务数据 |
| Account Delete | 必须存在完整入口和后端生命周期 |
| 客户端质量 | Huawei APMS |
| 后端质量 | Structured Logs + Metrics + OpenTelemetry |
| 上架测试 | 真机 + AGC 云调试 / 云测试 |

---

# 3. 最小总体架构

```text
                HarmonyOS App
                     │
          ┌──────────┴──────────┐
          │                     │
    Account Kit             Push Kit
          │                     │
 Authorization Code        getToken()
          │                     │
          ▼                     ▼
             FastAPI Backend
          ┌──────────┴──────────┐
          │                     │
 Auth / Session           Device API
          │                     │
          ▼                     ▼
      Internal User ──────── Device
          │                     │
          │                     │
          ▼                     │
 Personal State / Memory        │
 Thread / Run / File            │
 Automation                     │
          │                     │
          └───────┬─────────────┘
                  ▼
 NotificationIntent / Delivery / DurableJob
                  │
                  ▼
          Huawei Push Adapter
                  │
                  ▼
            Huawei Push Kit
                  │
         Lock / Banner / Center
                  │
                  ▼
              Deep Link
```

---

# 4. 登录方案

## 4.1 正式默认：Huawei Account

V2.2 主登录方式：

```text
Huawei Account
```

原因：

- HarmonyOS 原生用户路径短；
- 不需要老实人维护密码；
- 不需要短信发送体系；
- 支持华为账号登录和静默登录；
- 官方提供稳定 OpenID / UnionID。

## 4.2 使用 Authorization Code 服务端交换

推荐：

```text
HarmonyOS
↓
Account Kit 登录
↓
Authorization Code
↓
Backend
↓
Huawei Account server-side exchange
↓
UnionID / OpenID
↓
lookup/create Internal User
↓
Backend Business Session
```

客户端不得仅上传：

```text
union_id
open_id
user_id
```

然后让后端直接相信。

Backend 必须根据可信登录凭据完成服务端身份校验/交换。

## 4.3 UnionID / OpenID

V2.2 SHOULD 优先使用：

```text
UnionID
```

作为 Huawei external subject。

原因：

```text
UnionID
= 同一开发者账号下识别同一华为账号的稳定标识

OpenID
= 当前具体 HarmonyOS 应用内的用户标识
```

但业务表仍然只引用：

```text
internal user_id
```

## 4.4 Backend Business Session

完成 Huawei identity verification 后：

```text
Backend
→ issue business session credential
```

V2.2 冻结语义，不冻结实现：

```text
short-lived JWT?
opaque access token?
refresh token?
```

这些进入 Auth 实现阶段。

必须满足：

- 可失效；
- 可过期；
- 不把 Huawei credential 当业务 owner key；
- Backend middleware 能得到 stable internal user_id。

---

# 5. Minimal User

User 只承担账号根身份。

概念：

```text
User

user_id
status

created_at
updated_at
```

状态最低需要：

```text
ACTIVE
DELETING
DELETED
```

V2.2 不建设：

```text
bio
gender
birthday
social graph
complex preference profile
```

昵称 / 头像如果 UI 必须展示，可作为轻量 presentation/profile 字段或按用户授权获得，不上升成 Backend V2 核心领域。

---

# 6. External Auth Identity

概念：

```text
ExternalAuthIdentity

identity_id
user_id

provider = HUAWEI_ACCOUNT
subject_type = UNION_ID / OPEN_ID
subject

created_at
last_login_at
```

约束：

```text
provider + subject
MUST unique
```

所有 Huawei subject 严格按原值保存和比较。

---

# 7. Ownership

所有用户私有业务对象必须 owner-scoped：

```text
Thread
Message
Run
Thing
Task
ThingDate
File
Memory
Automation
Device
```

Backend 不信任客户端声明：

```text
user_id
owner_id
```

正确链：

```text
Business Session
↓
Auth Middleware
↓
AuthContext.internal_user_id
↓
Application / Repository ownership scope
```

Run / SSE / File 下载 / Interaction response 都必须验证 ownership。

---

# 8. Minimal Device

Device 不是完整设备管理平台，也不保存 Push Token。

它只承担：

```text
用户当前时区是什么
设备最近是否仍活跃
```

概念：

```text
Device

device_id
user_id

platform = HARMONYOS

timezone
last_seen_at

created_at
updated_at
```

关系：

```text
User 1 ─── N Device
```

多设备从一开始支持，不假设：

```text
one user = one push token
```

---

# 9. Device Registration

登录成功后：

```text
App
↓
建立/恢复 local device_id
↓
获取当前 timezone
↓
POST /devices/register
```

后端：

```text
AuthContext.user_id
+
device_id
+
timezone
```

建立 owner 关系。

`device_id` 不得被其他 user 静默抢占。

---

# 10. Notification Permission

## 10.1 Just-in-time

V2.2 不在首次启动 App 时无理由要求通知权限。

推荐：

```text
用户第一次创建 Reminder
↓
系统说明：
“为了在 App 关闭时提醒你，需要允许通知”
↓
请求系统通知权限
```

这比首次启动连续索取：

```text
通知
文件
麦克风
...
```

更符合最小权限和实际产品语义。

## 10.2 权限关闭不阻止 Reminder 创建

用户拒绝通知：

```text
Automation
仍可创建
```

但系统必须明确：

```text
当前设备无法通过系统通知提醒
```

不得假装提醒一定会送达。

---

# 11. Push Token Lifecycle

HarmonyOS 通过 Push Kit：

```text
pushService.getToken()
```

获取 token。

成功后：

```text
POST /devices/{device_id}/push-token
```

后端在独立 `PushEndpoint` 中 upsert 当前 Device 的 Push Token、启用状态与失效信息；`Device` 仅保存设备身份及 timezone、platform、last_seen 等设备级信息。

## 11.1 Token 不是永久值

必须支持：

```text
new token
token expired
app reinstall
device replacement
logout
notification unavailable
app uninstall signal / delivery failure
```

## 11.2 Logout / device detach

退出当前设备时：

```text
Backend disable device push binding
```

客户端 MAY 调用 Push Kit：

```text
deleteToken()
```

具体策略在客户端实现阶段确定。

## 11.3 Invalid token

Huawei Push 返回 token 不可用时：

```text
PushEndpoint.active = false
并记录 invalidated_at
```

不得持续无限重试旧 token。

---

# 12. V2.2 Automation 通知类型

## 12.1 REMINDER

用户明确要求的提醒：

```text
“明天下午三点提醒我提交。”
“截止前两天提醒我。”
```

## 12.2 CONDITION_MET

用户明确授权系统未来关注某条件：

```text
“官网出结果后告诉我。”
```

Condition Watch 成功后产生通知。

## 12.3 CONDITION_WATCH_ENDED

Condition Watch 因到期或达到检查上限而结束时产生的低打扰通知。

V2.2 暂不使用 `AGENT_RESULT` 作为 Automation 通知类型。未来若存在独立 Agent 异步结果通知需求，应作为其他通知场景扩展，不与 Automation Contract 混用。

暂不做：

```text
marketing
运营召回
广告
推荐
活动群发
```

---

# 13. Reminder 生产链

```text
User natural language
↓
Executive
↓
Automation
↓
Scheduler
↓
Occurrence
↓
latest State if needed
↓
NotificationIntent
↓
NotificationDelivery
↓
DurableJob
↓
HuaweiPushAdapter
↓
Huawei Push Kit
↓
Device
```

Automation / Scheduler 的完整执行模型仍由后续专项设计冻结。

本文只冻结：

> **Reminder 最终通知必须通过 `NotificationIntent + NotificationDelivery + DurableJob` 收敛，不能把 Scheduler transaction 与 Huawei HTTP 调用绑死。**

---

# 14. Notification durable boundary

本专项不新增独立 `NotificationOutbox` 表。Automation / Scheduler / Notification 领域的 authoritative contract 为：

```text
NotificationIntent
= 一次稳定、用户级、Provider-neutral 的通知事件

NotificationDelivery
= 一个 NotificationIntent 向一个具体 PushEndpoint 的外部送达身份

DurableJob
= delivery 的 claim / lease / retry / recovery 工作项
```

Delivery 状态统一为：

```text
READY
ACCEPTED
DELIVERED
FAILED
UNKNOWN_OUTCOME
CANCELLED
```

三者共同提供 transactional outbox 语义，但不复制一张 Outbox 表。

---

# 15. 通知事务边界

正确：

```text
BEGIN
Automation Occurrence durable
+
NotificationIntent
+
NotificationDelivery(s)
+
DurableJob(s)
COMMIT
↓
Notification Worker
↓
Huawei Push
```

而不是：

```text
BEGIN DB transaction
↓
调用 Huawei Push 等待网络
↓
COMMIT
```

目标：

```text
Push provider failure
≠
业务事务 rollback
```

---

# 16. Push Delivery Worker

Worker：

```text
claim PUSH_DELIVERY DurableJob
↓
load current PushEndpoint
↓
HuaweiPushAdapter
↓
record NotificationDelivery response
↓
ACCEPTED / DELIVERED / FAILED / UNKNOWN_OUTCOME
```

Retry 只针对明确 transient failure。

无效 token：

```text
invalidate PushEndpoint
```

永久参数 / 权限错误：

```text
不得盲目重试
```

---

# 17. 通知内容

第一版通知保持直接、真实、短。

示例：

```text
标题：
老实人提醒

正文：
软件杯报名明天截止。
```

禁止用虚假诱导式文案：

```text
“你有一条重大消息！”
```

但落地页实际无关。

通知标题、正文与目标业务必须一致。

---

# 18. Deep Link

V2.2 MUST 支持：

```text
Push
↓
tap
↓
open relevant context
```

最小目标类型：

```text
THING
TASK
THREAD
RUN_RESULT
```

推荐后端只生成稳定业务目标：

```text
target_type
target_id
```

客户端负责映射到具体页面路由。

不要把易变 UI 路由字符串长期存成业务真相。

---

# 19. Push 消息分类

老实人的用户主动 Reminder 属于：

```text
用户预期的服务提醒场景
```

上线前 MUST 在 Push Kit 后台检查并申请/配置适合的通知消息自分类权益。

原因：

- 华为对服务/通信、新闻/营销等分类采用不同提醒/频控策略；
- 未申请或类别使用错误可能落到默认营销通道；
- Reminder 不应被当成营销消息处理。

具体申请类别最终以 Huawei Push Kit 后台推荐和审核结果为准。

---

# 20. Delivery Receipt

## 20.1 V2.2 最低要求

MUST 知道：

```text
Push API request
→ accepted / failed
```

即：

```text
ACCEPTED / FAILED / UNKNOWN_OUTCOME
```

## 20.2 推荐上线支持

SHOULD 接 Huawei Push delivery receipt：

```text
provider sent
↓
device delivery status
↓
backend receipt endpoint
↓
notification delivery metadata
```

这可以帮助定位：

```text
token expired
notification disabled
network/device issue
frequency control
app uninstall
```

但：

```text
DELIVERED
≠
READ
```

系统不得声称用户已经阅读。

---

# 21. Logout

Logout：

```text
当前设备退出登录
```

不是账号删除。

流程：

```text
client logout
↓
backend invalidate current business session
↓
detach/disable current device push binding
↓
client clears local credential
```

以下继续存在：

```text
Things
Tasks
Memory
Files
Threads
Automation
```

用户以后用同一 Huawei identity 登录：

```text
→ same internal user_id
```

---

# 22. Account Deletion

V2.2 MUST 提供可发现的注销入口。

概念流程：

```text
Delete Account request
↓
user.status = DELETING
↓
block new Runs
↓
settle / cancel active Runs
↓
disable Automation
↓
disable Device / Push
↓
revoke sessions
↓
delete/anonymize user private data
↓
delete File objects when eligible
↓
finish
↓
user.status = DELETED / retained tombstone as legally necessary
```

具体数据保留 / 审计期限必须与最终隐私政策一致。

账号注销不能只：

```sql
DELETE FROM users
```

否则可能留下：

```text
Files
Memory
Automation
Push Token
Run
Object Storage
```

孤儿数据。

---

# 23. Privacy Data Inventory

从现在开始维护真实数据清单。

最低：

| 能力 | 数据 | 目的 |
|---|---|---|
| Huawei Account | UnionID/OpenID、必要登录信息 | 用户认证 |
| Backend Session | session metadata | 维持登录 |
| Device | device_id、timezone、last_seen | 多设备/时间语义 |
| Push Kit | push token、发送状态 | 系统提醒 |
| Chat | 用户消息 | Agent 对话 |
| Personal State | Thing/Task/Date 等 | 当前现实维护 |
| Memory | 长期用户知识 | 跨 Thread 连续性 |
| File | 用户上传资料及 metadata | 文件理解 |
| Runtime | Run / Tool / event metadata | 执行恢复 |
| Observability | 最小技术日志/trace metadata | 质量与故障定位 |

隐私政策必须和真实实现同步。

---

# 24. Privacy 最小要求

上线前必须确认：

```text
收集哪些个人信息
为什么收集
设备权限
第三方共享
第三方 SDK
个人信息管理方式
存储地点
保存期限
注销方式
联系方式
```

新增敏感数据处理/权限/SDK 后：

```text
Privacy Inventory
↓
Privacy Policy
```

必须同步更新。

---

# 25. 客户端质量支持

V2.2 SHOULD 使用 Huawei APMS 监控 HarmonyOS 客户端：

```text
Crash
Memory Leak
AppFreeze
performance indicators
```

职责划分：

```text
HarmonyOS client stability
→ Huawei APMS

Backend runtime
→ Structured Logs
  + Metrics
  + OpenTelemetry
```

不在 Backend 自建一套手机 Crash 平台。

---

# 26. 测试支持

首次正式上架前 SHOULD 完成：

```text
DevEco local
↓
physical Huawei device
↓
AGC Cloud Debugging
↓
AGC Cloud Testing
↓
small external/open test if needed
↓
release candidate
```

重点真机验证：

```text
Huawei Account login
silent/re-login
logout

notification permission
Push token
App killed receive Push
lock screen notification
Deep Link

network switch
offline / reconnect
account delete entry
```

---

# 27. 上线质量 Gate

## Gate U1 — Identity

- Huawei Account 真实登录成功；
- 新用户创建 internal user；
- 老用户重新登录回同一 user；
- 后端不信任客户端 user_id；
- A 用户不能读取 B 用户数据。

## Gate U2 — Device / Push

- Device 与 user 正确绑定；
- Push Token 可注册/更新/失效；
- App 不在前台仍可收到 Reminder；
- Token 无效后系统可收敛。

## Gate U3 — Reminder

- 用户自然语言创建 Reminder；
- Scheduler 到期；
- Outbox durable；
- Push 真实发送；
- 点击进入正确 Thing / Task / Thread。

## Gate U4 — Lifecycle

- Logout 不删除业务数据；
- Account Delete 阻止新 Run；
- Automation / Device / Push 正确停止；
- 用户数据清理路径可执行。

## Gate U5 — Release

- 隐私政策与真实行为一致；
- APMS 可看客户端质量；
- 关键机型完成真机/云测试；
- 无测试 token / dev user / debug credential 残留。

---

# 28. 最小 API Surface

最终路径名可在 Contract 专项调整，V2.2 只冻结能力。

```text
POST /auth/huawei/login
POST /auth/refresh?
POST /auth/logout

GET  /me
DELETE /me

POST /devices/register
PUT  /devices/{device_id}/push-token
DELETE /devices/{device_id}/push-token

POST /push/receipts?      # SHOULD，取决于 Huawei receipt 接入
```

提醒本身继续走 Automation API / Agent Tool，不新增：

```text
POST /notifications/send-random
```

给客户端任意发通知。

---

# 29. Security

必须：

- TLS；
- Huawei credentials / client secret 只在服务端；
- Push server credential 只在服务端；
- 客户端不能指定 owner_user_id；
- push token 视作敏感设备凭据；
- 日志不记录完整 access token / refresh token / push token；
- Delete Account / 高敏账号操作需要最新身份验证策略；
- 所有 Device API owner-scoped；
- Push receipt endpoint 验证 Huawei/provider 请求真实性（按官方能力实现）。

---

# 30. Redis 的使用边界

本专项不改变 Backend V2.2 Redis 定位。

Redis MAY 用于：

```text
auth/rate-limit transient state
short-lived session/cache optimization
Notification Worker wake-up
```

Redis MUST NOT 成为：

```text
User authority
Device authority
PushEndpoint truth
NotificationIntent / NotificationDelivery / DurableJob truth
```

这些 durable truth 仍在 PostgreSQL。

---

# 31. Failure Scenarios

## 31.1 Account Kit 暂时失败

```text
Login failure
→ clear user-facing retry
```

不得创建“半个已登录用户”。

## 31.2 Login transaction 成功、客户端响应丢失

再次登录：

```text
same Huawei subject
→ same internal user
```

不得重复创建 User。

## 31.3 Push Token 获取失败

用户仍可使用 Chat / State。

只标记：

```text
当前设备暂无系统通知能力
```

## 31.4 Push API timeout

Outbox 保持可重试状态。

不得重复创建 Automation occurrence。

## 31.5 Redis outage

Notification Worker 使用 PostgreSQL fallback polling。

Reminder 不丢。

## 31.6 App 被杀

Push 依赖 Huawei system push capability。

Backend Reminder 继续执行。

## 31.7 用户关闭系统通知

Automation 仍是事实。

UI 应能提示当前 Device 的通知不可用/可能无法提醒。

---

# 32. V1 → V2.2 最小迁移

当前开发态：

```text
fixed dev token / fixed user
Recording Push
```

目标：

```text
fixed dev identity
→ Huawei Account + internal user

single implicit device
→ minimal Device

Recording Push
→ real Huawei Push Adapter

no push token lifecycle
→ Device-linked PushEndpoint token

reminder execution
→ NotificationIntent + NotificationDelivery + DurableJob

generic app open
→ Deep Link target

client blind spot
→ APMS
```

不要求为这次迁移：

```text
重写 Personal State
重写 Memory
引入复杂 IAM
引入通知运营平台
```

---

# 33. 人工平台动作：与代码严格分开

以下事项需要开发者账号持有人在 Huawei Developer / AppGallery Connect 完成。

它们不是 Backend 功能。

## 33.1 鸿蒙应用开发者激励计划 2026

官方当前规则：

```text
报名截止：
2026-09-25

商户服务：
2026-09-30 前成功开通并通过审核

上架：
必须在计划期间正式上架
“仅提交审核”不算正式上架
```

官方还说明：

```text
现金激励按首次正式上架时间先到先得
```

并在 FAQ 明确建议：

```text
至少预留 1 周用于应用审核和整改
```

### 项目内部建议节点

以下是**老实人内部计划，不是 Huawei 官方截止日期**：

```text
现在 ～ 09-05
完成激励报名 / 商户服务申请 / AGC 服务准备

09-10 前
完成 Huawei Account + Push 真机闭环

09-15 前
形成 Release Candidate
完成隐私 / 素材 / 云测试

09-20 前
力争首次正式提审

09-20 ～ 正式上架
只做审核整改和 P0 修复
```

不要把首次提审拖到 9 月底。

---

# 34. 需要你本人操作的最小清单

```text
[ ] 确认 Huawei Developer 开发者实名认证状态
[ ] 创建/确认 AppGallery Connect 应用
[ ] 报名 2026 鸿蒙应用开发者激励计划
[ ] 尽快开通并提交商户服务审核
[ ] 开通/配置 Account Kit
[ ] 开通/配置 Push Kit
[ ] 申请/确认适合 Reminder 的 Push 消息分类权益
[ ] 准备应用名称、图标、截图、简介
[ ] 配置/确认隐私政策
[ ] 使用云调试 / 云测试验证
[ ] 上传 Release 包
[ ] 首次提交审核
[ ] 根据审核结果整改
[ ] 确认应用已“正式上架”，不是仅“审核中/已提交”
```

这些平台动作由账号持有人最终确认和提交。

---

# 35. 本阶段明确不做

为了保证首次上线节奏，以下推迟：

```text
手机号登录
邮箱登录
密码登录
复杂账号 linking
Account Center
复杂 Device 管理 UI

营销 Push
Push Campaign
A/B Testing
人群标签
短信/邮件
实况窗
通知运营后台
用户已读追踪

复杂 delivery analytics
自建客户端 Crash 平台
```

---

# 36. 后续专项边界

本设计完成后：

```text
Personal State & Memory
→ 继续定义现实与长期知识

Tool / API / Policy
→ 定义 Executive 如何安全调用业务

Automation / Scheduler / Push
→ 详细冻结 Schedule、Occurrence、Condition Watch、
   NotificationDelivery retry、Huawei Push Adapter
```

本文已经冻结的用户/Device/Push 最小边界不得在后续专项中无理由扩大。

---

# 37. Frozen Decisions

V2.2 正式冻结：

1. Huawei Account 是首次上线主登录方式；
2. 使用服务端可信登录交换，不相信客户端自报 user_id；
3. 优先使用 UnionID 作为 Huawei external subject；
4. 所有业务对象 owner 到 internal user_id；
5. Account Kit + Backend Business Session 足够，AGC Auth 不作为 V2.2 必选依赖；
6. User 保持极简，不建设 Profile Domain；
7. User 1:N Device；
8. Device 只承担 Push / timezone / last_seen 等最小职责；
9. 通知权限 Just-in-time 请求；
10. Push Token 归属独立 PushEndpoint、关联 Device，且可更新/失效；
11. Automation 通知类型为 REMINDER / CONDITION_MET / CONDITION_WATCH_ENDED；
12. `NotificationIntent + NotificationDelivery + DurableJob` 是 Push durable 边界，不新增独立 NotificationOutbox；
13. Push provider failure 不 rollback Automation；
14. Reminder Push MUST 支持业务 Deep Link；
15. 上线前必须配置正确的 Huawei Push 消息分类权益；
16. MUST 记录 Push send result；SHOULD 接 delivery receipt；
17. Logout ≠ Account Delete；
18. Account Delete 必须收敛 Run / Automation / Device / Push / user data；
19. 隐私政策与真实数据行为同步维护；
20. HarmonyOS 客户端质量使用 APMS；
21. 后端继续使用 Logs + Metrics + OpenTelemetry；
22. 正式上架前使用真机 / AGC 云调试 / 云测试；
23. Redis 不是 User / Device / Outbox truth；
24. 激励报名、商户服务、提审属于人工平台动作，不混入 Backend Domain。

---

# 38. 官方依据

以下均为 Huawei 官方资料，用于校验上线能力与规则；项目自己的领域设计仍以上位架构为准。

## 38.1 2026 鸿蒙应用开发者激励计划

https://developer.huawei.com/consumer/cn/activity/harmonyos-incentive/2026

关键依据：

- 2026-04-15 ～ 2026-09-25 报名；
- 2026-09-30 前商户服务成功开通并通过审核；
- 必须在计划期间正式上架至 HarmonyOS 5.0 及之后的华为应用市场；
- 仅提交上架申请、未审核通过不满足要求；
- 官方建议至少预留 1 周审核和整改；
- 现金激励先到先得。

## 38.2 Account Kit

登录能力概述：

https://developer.huawei.com/consumer/cn/doc/doccenter-capabilities/account-quick-login-overview

Huawei Account 自定义按钮 / 服务端 Authorization Code 流程：

https://developer.huawei.com/consumer/cn/doc/doccenter-capabilities/account-unionid-login-api

Account Kit SDK：

https://developer.huawei.com/consumer/cn/sdk/account-kit

官方说明：

- Huawei Account login / silent login 支持个人和企业开发者；
- UnionID / OpenID 是 Huawei Account 用户身份标识；
- 官方建议同开发者账号下跨应用识别用户时使用 UnionID；
- 服务端 Authorization Code 流程可用于建立应用自己的业务登录凭证。

## 38.3 AGC Auth（参考，不作为 V2.2 必选）

服务端验证用户凭据：

https://developer.huawei.com/consumer/cn/doc/appgallery-connect-Guides/agc-auth-server-verifytoken-0000001727548362

服务端认证说明：

https://developer.huawei.com/consumer/cn/doc/AppGallery-connect-Guides/agc-auth-server-introduction-0000001727388954

用于说明：

```text
移动客户端不是可信环境
Backend 必须验证可信认证凭据
```

V2.2 默认不为了这一点再额外构建双重认证栈。

## 38.4 Push Kit

pushService：

https://developer.huawei.com/consumer/en/doc/harmonyos-references-V13/push-pushservice-V13

包含：

```text
getToken
deleteToken
bind/unbind profile
receive message
```

Push delivery FAQ：

https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V13/push-faq-1-V13

官方指出：

- 云侧发送成功后，设备侧仍可能因为通知开关、网络、卸载、Token 过期、频控等原因不展示；
- 推荐开发消息回执获得终端送达状态。

通知消息自分类：

https://developer.huawei.com/consumer/cn/doc/doccenter-capabilities/push-skill-classification

官方说明未申请或错误使用分类时可能走默认营销权益，不同类别具有不同频控规则。

## 38.5 Privacy

HarmonyOS 应用隐私政策：

https://developer.huawei.com/consumer/cn/doc/doccenter-submission/agc-help-privacy-policy-app-0000002282162168

隐私政策需要覆盖：

```text
个人信息收集与使用
设备权限
第三方共享
第三方 SDK
个人信息管理
存储地点及期限
联系方式
```

并且实际数据处理变化后需要同步更新。

## 38.6 APMS / Cloud Test

APMS 稳定性分析：

https://developer.huawei.com/consumer/cn/doc/best-practices/bpta-stability-analysis-in-operation

官方覆盖：

```text
Crash
Memory Leak
AppFreeze
```

云调试：

https://developer.huawei.com/consumer/cn/doc/App/agc-help-clouddebug-appointequip-0000002255019572

激励计划 FAQ 同时明确：

```text
可使用 AGC 云测试 / 云调试远程真机
可使用 APMS 查看现网 APP 质量和崩溃
```

---

# 39. 一句话定义

> **老实人 V2.2 的最小用户与通知支持，是用 Huawei Account 建立可信登录并映射到稳定 internal user_id，用轻量 Device 管理设备身份与 timezone、用独立 PushEndpoint 管理 Token，并通过 Automation → NotificationIntent → NotificationDelivery → DurableJob → Huawei Push Kit 实现 App 关闭后的真实提醒，再以 Deep Link、注销、隐私、APMS 和上架测试补齐一个消费者 HarmonyOS App 首次正式发布所必需的生产闭环，而不建设完整账号平台和通知运营平台。**
